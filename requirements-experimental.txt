# Experimental requirements for auto-subtitle.
#
# This tier adds NVIDIA NeMo for the Parakeet ASR backend. It is large,
# pulls in heavy ML dependencies, and is not required for the default
# faster-whisper pipeline. Opt in with:
#
#     pip install -r requirements-experimental.txt
#
# Or, if installing the project itself:
#
#     pip install -e ".[experimental]"

-r requirements.txt

nemo_toolkit[asr]
